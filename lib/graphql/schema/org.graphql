type Org {
  id: ID!
  name: String!
  slug: String!
  settings: JSON
  createdAt: DateTime!
  updatedAt: DateTime!

  # Relations
  members: [OrgMember!]!
  clients: [Client!]!
  integrations: [Integration!]!

  # Computed
  clientCount: Int!
  memberCount: Int!
}
